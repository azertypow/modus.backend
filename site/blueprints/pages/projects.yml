title: Projects

status:
  listed: true

options:
  changeSlug: false
  delete: false
  changeTitle: true
  changeStatus: false

columns:
  - width: 1/1
    fields:
      content:
        extends: fields/body
        fields:
          headerImage:
            required: true
          body: false
  - width: 2/1
    sections:
      pages:
        type: pages
        create: project
        templates:
          - project
